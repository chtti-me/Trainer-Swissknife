node_modules/
.next/
out/
build/
*.tsbuildinfo
next-env.d.ts

# ============ 環境變數（含密鑰，絕對不可上 git）============
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# ============ 舊版 SQLite 資料庫（含實際使用者資料，不可上 git）============
prisma/dev.db
prisma/dev.db-journal
prisma/dev.db.*backup*.db
prisma/*.sqlite
prisma/*.sqlite3

# ============ 日誌與暫存 ============
npm-debug.log*
yarn-debug.log*
yarn-error.log*
*.log
.DS_Store
Thumbs.db

# ============ 編輯器／系統 ============
.vscode/
.idea/
*.sw?
.vercel

# ============ v3 Agent 工作區（使用者透過 Agent 讀寫的檔案）============
agent-workspace/

# ============ 其他暫存 ============
*.local
.cache/
coverage/

# ============ 根目錄開發過程殘留圖檔（未被程式碼引用，不入版控）============
# 若之後要把 logo / favicon 正式放進專案，請改放到 public/ 並從程式碼引用
/瑞士刀-*.png
/瑞士刀.png
/瑞士刀.ico
# Windows 下載檔附帶的 NTFS Alternate Data Stream 標記（git 無法處理含冒號檔名）
*:Zone.Identifier
